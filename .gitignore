# Rust build output
/target

# Packaging build artifacts (regenerated by scripts/make-appimage.sh + make-windows.{sh,ps1}):
# downloaded tools/binaries, the MSVC SDK splat, staged AppDir/bundle, and packaged output.
/build
/squashfs-root
/*.AppImage
/*-win64.zip

# OS / editor cruft
.DS_Store
*.swp
