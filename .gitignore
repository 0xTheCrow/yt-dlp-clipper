# Rust build output
/target

# AppImage build artifacts (regenerated by scripts/make-appimage.sh):
# downloaded tools/binaries, the staged AppDir, and the packaged output.
/build
/squashfs-root
/*.AppImage

# OS / editor cruft
.DS_Store
*.swp
